To detect posture across the ambulatory recordings, per participant, run the scripts in order (1 to be run with MATLAB when the VU-DAMS file of the participant is open in the background), 2 to be run with Jupyter Notebook interface (Python), 3 to be run with Spyder (Python).

